/** Reported in the `User-Agent` of every API call. M2.6 (packaging) stamps the release version here. */
export const COMPANION_VERSION = '0.0.0';

export const USER_AGENT = `customs-night-companion/${COMPANION_VERSION} (${process.platform})`;
