/**
 * PLACEHOLDER. Regenerate with `pnpm db:types` once M1.2 has applied `0001_init.sql`.
 * Do not hand-edit: the real contents are `supabase gen types typescript` output.
 */

export type Json = string | number | boolean | null | { [key: string]: Json | undefined } | Json[];

export type Database = Record<string, never>;
