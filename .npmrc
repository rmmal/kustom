# Node 25 is newer than some tools' declared engine ranges. Warn, do not fail.
engine-strict=false
# Next.js and Biome ship optional platform binaries; hoist nothing else.
strict-peer-dependencies=false
